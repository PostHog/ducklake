# VENDORED COPY of duckdb/extension-ci-tools/.github/workflows/_extension_deploy.yml
# Source SHA: 344397bb848aac786d260daf396ad09fc6a1d21f (v1.5.2)
# Reason: PostHog org policy requires all action uses to be pinned to full
# commit SHAs, which the upstream copy of this reusable workflow violates.
# To update: re-run scripts/vendor-extension-ci-tools.sh (or equivalent) to
# pull a newer upstream SHA and re-apply the SHA pins for actions referenced
# below.
#
# Reusable workflow that deploys the artifacts produced by .github/workflows/_extension_distribution.yml
#
# Note: this workflow can be used to deploy an extension from a GH artifact. It is intended for Out-of-tree
#       extensions that live the duckdb GitHub organisation (only repo's within the same organisation can share secrets
#       through reusable workflows)


name: Extension Deployment
on:
  workflow_call:
    inputs:
      # The name of the extension
      extension_name:
        required: true
        type: string
      # DuckDB version to build against
      duckdb_version:
        required: true
        type: string
      # The version of the https://github.com/duckdb/extension-ci-tools submodule of the extension. In most cases will be identical to `duckdb_version`.
      # Passing this explicitly is required because of https://github.com/actions/toolkit/issues/1264
      ci_tools_version:
        required: true
        type: string
      # Override the repo for the CI tools (for testing CI tools itself)
      override_ci_tools_repository:
        required: false
        type: string
        default: "duckdb/extension-ci-tools"
      # ';' separated list of architectures to exclude, for example: 'linux_amd64;osx_arm64'
      exclude_archs:
        required: false
        type: string
        default: ""
      # ';' separated list of architectures to opt into, for example: 'windows_arm64;linux_arm64_musl'
      opt_in_archs:
        required: false
        type: string
        default: ""
      # Whether to upload this deployment as the latest. This may overwrite a previous deployment.
      deploy_latest:
        required: false
        type: boolean
        default: false
      # Whether to upload this deployment under a versioned path. These will not be deleted automatically
      deploy_versioned:
        required: false
        type: boolean
        default: false
      # Postfix added to artifact names. Can be used to guarantee unique names when this workflow is called multiple times
      artifact_postfix:
        required: false
        type: string
        default: ""
      # Override the default deploy script with a custom script
      deploy_script:
        required: false
        type: string
        default: "./duckdb/scripts/extension-upload-single.sh"
      # Override the default matrix parse script with a custom script
      matrix_parse_script:
        required: false
        type: string
        default: "./extension-ci-tools/scripts/modify_distribution_matrix.py"
      # auto/enabled/disabled: when enabled (or enabled through auto) will skip redundant architectures to reduce CI load
      reduced_ci_mode:
        required: false
        type: string
        default: 'auto'

jobs:
  generate_matrix:
    name: Generate matrix
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      deploy_matrix: ${{ steps.parse-matrices.outputs.deploy_matrix }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        name: Checkout Extension CI tools
        with:
          path: 'extension-ci-tools'
          ref: ${{ inputs.ci_tools_version }}
          repository: ${{ inputs.override_ci_tools_repository }}

      - id: parse-matrices
        env:
          MATRIX_PARSE_SCRIPT: ${{ inputs.matrix_parse_script }}
          EXCLUDE_ARCHS: ${{ inputs.exclude_archs }}
          OPT_IN_ARCHS: ${{ inputs.opt_in_archs }}
          REDUCED_CI_MODE: ${{ inputs.reduced_ci_mode }}
        run: |
          python3 "$MATRIX_PARSE_SCRIPT" --input extension-ci-tools/config/distribution_matrix.json --deploy_matrix --output deploy_matrix.json --exclude "$EXCLUDE_ARCHS" --opt_in "$OPT_IN_ARCHS" --pretty --reduced_ci_mode "$REDUCED_CI_MODE"
          deploy_matrix="`cat deploy_matrix.json`"
          echo deploy_matrix=$deploy_matrix >> $GITHUB_OUTPUT
          echo `cat $GITHUB_OUTPUT`

  deploy:
    name: Deploy to nightly
    runs-on: ubuntu-latest
    permissions:
      contents: read
    needs: generate_matrix
    if: ${{ needs.generate_matrix.outputs.deploy_matrix != '{}' && needs.generate_matrix.outputs.deploy_matrix != '' }}
    strategy:
      matrix: ${{fromJson(needs.generate_matrix.outputs.deploy_matrix)}}

    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          fetch-depth: 0
          submodules: 'recursive'

      - name: Checkout DuckDB to version
        env:
          DUCKDB_VERSION_INPUT: ${{ inputs.duckdb_version }}
        run: |
          if [ ! -d duckdb ]; then
            git clone https://github.com/duckdb/duckdb.git
          fi
          cd duckdb
          git checkout "$DUCKDB_VERSION_INPUT"

      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: ${{ inputs.extension_name }}-${{ inputs.duckdb_version }}-extension-${{matrix.duckdb_arch}}${{inputs.artifact_postfix}}${{startsWith(matrix.duckdb, 'wasm') && '.wasm' || ''}}
          path: |
            /tmp/extension

      - name: Deploy
        shell: bash
        env:
          AWS_ENDPOINT_URL: ${{ secrets.DUCKDB_EXTENSIONS_NIGHTLY_S3_ENDPOINT }}
          AWS_ACCESS_KEY_ID: ${{ secrets.DUCKDB_EXTENSIONS_NIGHTLY_S3_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.DUCKDB_EXTENSIONS_NIGHTLY_S3_KEY }}
          BUCKET_NAME: 'duckdb-extensions-nightly'
          DUCKDB_EXTENSION_SIGNING_PK: ${{ secrets.S3_DUCKDB_ORG_EXTENSION_SIGNING_PK }}
          DUCKDB_DEPLOY_SCRIPT_MODE: for_real
          PIP_BREAK_SYSTEM_PACKAGES: 1
          DEPLOY_SCRIPT: ${{ inputs.deploy_script }}
          EXTENSION_NAME: ${{ inputs.extension_name }}
          DUCKDB_ARCH: ${{ matrix.duckdb_arch }}
          DEPLOY_LATEST: ${{ inputs.deploy_latest || 'true' && 'false' }}
          DEPLOY_VERSIONED: ${{ inputs.deploy_versioned || 'true' && 'false' }}
        run: |
          pwd
          python3 -m pip install awscli
          git config --global --add safe.directory '*'
          cd duckdb
          git fetch --tags
          export DUCKDB_VERSION=`git tag --points-at HEAD`
          export DUCKDB_VERSION=${DUCKDB_VERSION:=`git log -1 --format=%h`}
          cd ..
          git fetch --tags
          export EXT_VERSION=`git tag --points-at HEAD`
          export EXT_VERSION=${EXT_VERSION:=`git log -1 --format=%h`}
          "$DEPLOY_SCRIPT" "$EXTENSION_NAME" "$EXT_VERSION" "$DUCKDB_VERSION" "$DUCKDB_ARCH" "$BUCKET_NAME" "$DEPLOY_LATEST" "$DEPLOY_VERSIONED"
